import React from 'react'
import styled from 'styled-components'

const STitle = styled.div`
  display: block;
  text-align: center;
`

export const Title = (props) => {
  return <STitle>yo</STitle>
}

export default Title
